package com.company.OOPs;

/**
 * Private : the access level of a private modifier is only within the class.
 *           It cannot be accessed from outside the class.
 *
 * Default : only within the package. if you do not specify any access level,it will be the default.
 *
 * Protected : the access level of a protected modifier is only within the package
 *           and outside the package through child class. If we donot make the child
 *           class it will not be accessed from outside the package.
 * Public : The access level of a public modifier is everywhere.
 *
 */
public class AccessModifiers {
}
